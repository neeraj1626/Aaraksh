services:
  - type: web
    name: health-backend
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y tesseract-ocr
    startCommand: gunicorn app:app
    runtime: python
    pythonVersion: 3.10.13   # ✅ ADD THIS
